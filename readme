pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

pip install ultralytics opencv-python numpy

verificacion 
python -c "import torch; print(torch._version_); print('GPU disponible:', torch.cuda.is_available()); print('GPU:', torch.cuda.get_device_name(0))"

descargar videos 
yt-dlp -o "data/videos/test1.mp4" "URL_QUE_COPIASTE"
